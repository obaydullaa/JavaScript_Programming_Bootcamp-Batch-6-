/**
 * 09.4_Finding element
 */

// Finding element

const numbers = [1, 3, 5, 8, 10];

// const includedOrNot = numbers.indexOf("777");
// const rejult = (includedOrNot === -1) ? 'Not inclided' : 'Inclided';

const resultInclude = numbers.includes(3);

console.log(resultInclude);