/**
 * 9.3_Removing elements From a array
 */

// Removing Elements

const numbers = [1, 3, 5, 8, 10];
// Ending Remove
numbers.pop();

// Begnning Remove
numbers.shift();

//Middle remove
numbers.splice(2, 2);
console.log(numbers);