/**
 * 09.14_every and some helper
 */

/**
 * Return true OR False 
 * every chek every item and if every item true then return true.
 * some return any item true then return true.
 */

const numbers = [1, 7, 3, 9];

const rejult = numbers.every(number => number >= 1);

console.log(rejult);