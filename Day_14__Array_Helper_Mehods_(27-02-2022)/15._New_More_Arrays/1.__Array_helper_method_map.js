/**
 * 1.Array helper method map
 * ---------------------------
 * map() মেথডটি একটি নতুন array তৈরি করে, যেখানে প্রতিটি এলিমেন্টের উপর নির্দিষ্ট ফাংশন প্রয়োগ করা হয়। এটি মূল array পরিবর্তন করে না।
 */

// Map 
// It works with an array 
// return an array with all (?) substitutes 
// results depends ho you manipulate the data
// must return from the call back function 
// doesn't mutate the original array

// let arr = [1, 2, 3, 4];

// let result = arr.map(function (elm){
//     console.log(elm)
//     return elm * 2;
// });

// console.log(result);
// console.log(arr);

// let numbers = [1, 2, 3, 4, 5];


// let doubled = numbers.map((num) => {
//     return num * 2;
// });

// console.log(doubled);  // Output: [2, 4, 6, 8, 10]