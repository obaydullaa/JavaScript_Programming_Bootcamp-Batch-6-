/**
 * 6.8.For of loop
 */

const profile = ['Obaydul', 'Islam', 25, 'Weg Programmer'];
// way one
for (let value of profile) {
    console.log(value);
}

// way two
for (value of profile) {
    console.log(value);
}