/**
 * 6.6.Looping with array
 */

const profile = ['Obaydul', 'Islam', 24, 'Weg Programmer'];

let i = 0;
while (i < profile.length) {
    console.log(profile[i]);
    i++;
}

for (let i = 0; i < profile.length; i++) {
    console.log(profile[i]);
}