/**
 * 6.3.Infinite loop
 */

const text = 'Allah Mohan';
let number = 1;
while (number < 6) { // It will be false any times, It it's not false any times it will be infinite loop
    console.log(`${text} Reapeated in ${number} times`);
    // number++;
}